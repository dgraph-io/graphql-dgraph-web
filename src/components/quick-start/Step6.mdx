import GraphiQLWrapper from "../GraphiQL"
import {quickStartQry2} from "../../utils/queries"

Maybe you want to find reviews that describe best GraphQL products and give a high rating.

<GraphiQLWrapper defaultQuery={quickStartQry2} />

export default ({ children }) => <>{children}</>
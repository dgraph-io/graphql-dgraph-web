import GraphiQLWrapper from "../GraphiQL"

It's a one-liner to bring up Dgraph with GraphQL. _Note: The Dgraph standalone image is great for quick start and exploring, but it's not meant for production use. Once you want to build an App or persist your data for restarts, you'll need to review the [admin docs](#)_.

<GraphiQLWrapper />

export default ({ children }) => <>{children}</>

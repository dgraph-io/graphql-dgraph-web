**FIXME:** Not sure if this needs multiple pages or a single page.  At least needs:

* a quick description of subscriptions work (see Apoorv's blog)
* description of how to add it to our schemas
* an example of a schema
* Is there a dev tool that can do subscriptions? Insomnia, firecamp, Altair, Postman??? If so we could have an  example of that.
* probably also needs a description of how it works with Apollo Client (see Apoorv's blog)